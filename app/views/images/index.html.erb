<h1>All Images</h1>
<table>
  <tr>
    <th>URL</th>
    <th>Image</th>
  </tr>
 
  <% @images.each do |image| %>
    <tr>
      <td><%= image.url %></td>
      <td><%= image_tag image.url, class:'max-dimensions' %></td>
    </tr>
  <% end %>
</table>
