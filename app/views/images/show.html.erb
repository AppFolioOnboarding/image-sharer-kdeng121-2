<h1>Your image</h1>
<%= image_tag @image.url, class: 'max-dimensions' %>
<strong>Tags:</strong>
  <% @image.tag_list.each do |tag| %>
    <span class="js-tag"><%= link_to tag, images_path(tag: tag)%></span>
  <% end %>
